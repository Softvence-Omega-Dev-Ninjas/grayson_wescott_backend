services:
  server:
    image: sajibsv/grayson-server:latest
    platform: linux/amd64
    container_name: grayson-server-api
    restart: always
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      BASE_URL: https://lgcglobalcontractingltd.com/js

      PORT: 5010
      NODE_ENV: production

      DATABASE_URL: postgresql://postgres:postgres@db:5432/grayson_db

      JWT_SECRET: asdfkamskfmasklfmaksmfaskfmaksfaopsfmpaomsfpaf
      JWT_EXPIRES_IN: 90d

      MAIL_USER: arafatmannan2019@gmail.com
      MAIL_PASS: eurp zvyi gnoy enag

      SUPER_ADMIN_EMAIL: 'grayson@gmail.com'
      SUPER_ADMIN_PASS: 'superadmin'

      REDIS_URL: redis://redis:6379
      REDIS_HOST: redis
      REDIS_PORT: 6379

      OAUTH_CLIENT_ID: '1074646902032-5ab3gkkggb7mrejjfnvr7ra65qos4o8s.apps.googleusercontent.com'
      OAUTH_CLIENT_SECRET: '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'

      FRONTEND_VERIFICATION_URL: 'https://grayson-wescott-frontend.vercel.app/twostepverification'
      FRONTEND_RESET_PASSWORD_URL: 'https://grayson-wescott-frontend.vercel.app/resetpassword'
      FRONTEND_SOCIAL_EMAIL_URL: 'https://grayson-wescott-frontend.vercel.app/socialemail'

      TWILIO_ACCOUNT_SID: ACd694ffdbf32281b95c8cbf5adc844719
      TWILIO_AUTH_TOKEN: efa5b0c1c86f9784b255ffe07497c618
      TWILIO_PHONE_NUMBER: +12543554726
    ports:
      - 5010:5010
    depends_on:
      - db
      - redis
    command: >
      sh -c "
        npx prisma migrate deploy &&
        npm run start
      "

  db:
    image: postgres
    container_name: grayson_db
    restart: always
    # user: postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: grayson_db
    volumes:
      - grayson_db:/var/lib/postgresql/data
    ports:
      - '5404:5432'

  redis:
    image: redis
    container_name: grayson_redis
    restart: always
    ports:
      - '6384:6379'

volumes:
  grayson_db:
